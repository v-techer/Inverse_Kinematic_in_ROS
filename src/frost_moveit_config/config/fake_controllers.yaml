controller_list:
  - name: fake_frost_arm_controller
    type: $(arg execution_type)
    joints:
      - joint0
      - joint1
      - joint2
      - joint3
      - joint4
      - joint5
  - name: fake_frost_hand_controller
    type: $(arg execution_type)
    joints:
      - left_gripper_joint
      - right_gripper_joint
initial:  # Define initial robot poses.
  - group: frost_arm
    pose: straight